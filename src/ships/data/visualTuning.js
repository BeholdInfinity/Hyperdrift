/**
 * Ship visual tuning — machine-editable via Dev Mode Blueprint Author.
 * Loaded into SHIP.* at startup; Save overwrites this file.
 */

export const VISUAL_TUNING = {
  thrusterCupScale: 1.5,
  thrusterPlumeScale: 1.15,
  /** Multiplier on classScale for generic (non-bell) engine draw */
  genericEngineClassScale: 1,
};
